<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.salao">
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Salao">
        <activity android:name=".TipoLoja"
            android:parentActivityName=".TelaPrincipal"></activity>
        <activity android:name=".TipoPromocoes"
            android:parentActivityName=".TelaPrincipal"></activity>
        <activity android:name=".ServicosManicure"
            android:parentActivityName=".TelaPrincipal"></activity>
        <activity android:name=".ServicosCabelereiro"
            android:parentActivityName=".TelaPrincipal"></activity>
        <activity android:name=".TelaPrincipal" />
        <activity android:name=".Home" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>